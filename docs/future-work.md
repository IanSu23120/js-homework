# Future Work

## 拖曳排序

使用已安裝的 `@dnd-kit`，讓同一天行程可以拖曳調整順序。

## 費用管理

可加入：

- 交通
- 住宿
- 餐飲
- 門票
- 購物
- 其他

後續可用 Recharts 做分類統計圖。

## 匯率換算

可串接 ExchangeRate API。若需要 API key，應放在 `.env.local`，並以 `VITE_` 前綴命名。

## 匯出功能

目前已有複製文字摘要。可擴充：

- 下載 JSON
- 下載 Markdown
- 匯出 CSV

## 更多城市與景點

目前城市與景點是靜態資料。未來可考慮：

- 擴充更多亞洲城市
- 依類別篩選
- 依旅遊天數推薦
- 接入外部景點 API

## 後端同步

目前使用 localStorage。未來若要跨裝置同步，可加入：

- 使用者登入
- 後端 API
- 雲端資料庫

## PWA

可將專案擴充為 PWA：

- 離線快取
- 加到手機桌面
- 行程離線查看
